# Active Learning Framework

Учебный фреймворк для активного обучения с расширенной визуализацией и настройкой параметров, сонованный на статьe
"Model-Centric and Data-Centric Aspects of Active Learning for Deep Neural Networks"

## Быстрый старт

```python
from active_learning import ActiveLearning, ANN, plot_active_learning_results

# Инициализация модели и AL
model = ANN(input_dim=784, hidden_dim=128, output_dim=10)
al = ActiveLearning(
    model=model,
    X_train=X_train, y_train=y_train,
    X_test=X_test, y_test=y_test,
    strategy="entropy",
    al_type="cumulative"
)

# Запуск обучения
al.fit(lr=0.01)

# Визуализация
plot_active_learning_results(al, dataset_name="MNIST")
plot_active_learning_results_many(al, dataset_name="MNIST", name="AL Results Graphic")




🔧 Класс ActiveLearning

Основные атрибуты:

model - нейросетевая модель для обучения

strategy - стратегия выборки: "random", "entropy", "confidence", "margin"

al_type - тип обучения: "cumulative", "incremental", "half-cumulative"

update_size - размер батча для обновления

init_size - начальный размер размеченных данных

test_metrics - история метрик на тестовой выборке

labeled_fractions - история доли размеченных данных

most_inf_images - список наиболее информативных изображений


Ключевые методы:

fit() - основной цикл активного обучения

_acquisition_function() - функция acquisition для выборки данных

predict(), predict_proba() - инференс модели



🧠 Класс ANN

Простая полносвязная нейросеть:

input_dim - размерность входных данных

hidden_dim - размер скрытого слоя

output_dim - количество классов



📈 Функции визуализации

plot_active_learning_results(al, dataset_name, title)
Создает интерактивный HTML-график с:

- Траекторией обучения модели

- Всплывающими подсказками с метриками

- Просмотром наиболее информативных изображений

- Возможностью закрепления точек на графике


plot_active_learning_results_many(*al_objects, legend, dataset_name, title, name)
Строит сравнительный график для нескольких AL-экспериментов:

- Оптимизирован для большого количества точек (~15k)

- Стилизация для научных публикаций

- Сохранение в PDF формате

- Кастомная цветовая палитра



📁 Структура проекта

autumn_school_15_active_learning/
├── src/
│   ├── actlearn.py
│   └── visualization.py
├── notebooks/
│   ├── data/
│   │   ├── FashionMNIST
│   │   └── MNIST
│   ├── AL_Brain_Tumour_MRI.ipynb
│   └── AL_MNIST.ipynb
├── graphics/
│   ├── BrainTumourMRI
│   ├── FashionMNIST
│   └── MNIST
├── requirements.txt
└── README.md

